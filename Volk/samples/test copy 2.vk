class Test
{
    int _size;
    int _count;

    constructor (int size, int count)
    {
        this._size = size;
        this._count = count;
    }

    function int GetSize()
    {
        return this._size;
    }
}

Test var = new Test(420, 2);
printf("var._size: %d \n", var.GetSize());
return 42;